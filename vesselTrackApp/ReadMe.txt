Vessel Track App

Getting Started:
Download and install node.js
Command Window: npm install –g (create-react-app)
Command Window: cd myapp
Command Window: npm start.
At the browser: localhost:3000

I couldn't render anything on the map
rather than google's map marker. So I use the marker
as a vessel and render anything else below google map
or at the modal.